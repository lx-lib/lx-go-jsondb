services:
  postgres:
    image: postgres:18-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: test
      POSTGRES_PASSWORD: test
    volumes:
      - ./init:/docker-entrypoint-initdb.d